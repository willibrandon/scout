# Scout Implementation Packages

Scout implementation packages provide the reusable assemblies behind the public Scout library entry points.

Most applications should reference one of these packages:

- `Scout.Text.Regex`
- `Scout.IO.Globbing`
- `Scout.IO.Ignore`

The implementation packages are versioned with Scout and remain Native-AOT-compatible.
